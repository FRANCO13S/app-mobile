.modal-container {
  background-color: rgb(255, 255, 255);
  border-radius: 8px;
  box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
  width: 90%;
  max-width: 400px;
  padding: 20px;
  margin-top: 90px;
  margin-left: 22px;
  text-align: center;
}

.presentation {
  text-align: center;
  padding: 20px;
}

.presentation h1 {
  font-size: 24px;
  color: var(--ion-color-primary);
}

.presentation p {
  font-size: 16px;
  color: var(--ion-color-medium);
}

img.logo {
  height: 40px; /* Ajusta la altura del logo */
  margin-left: 10px; /* Margen para separarlo del borde */
}





import { IonicModule } from '@ionic/angular';
/* eslint-disable @angular-eslint/no-empty-lifecycle-method */
import { Component, OnInit } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-tabinicial',
  templateUrl: './tabinicial.page.html',
  styleUrls: ['./tabinicial.page.scss'],
  standalone: true,
  imports: [IonicModule ,CommonModule, FormsModule, RouterLink]
})
export class TabinicialPage implements OnInit {
  showPresentation = false; // Inicialmente muestra la sección principal

  credenciales = {
    correo: '',
    contra: ''
  };
  constructor() { }

  ngOnInit() {
  }

  login() {
    // Lógica para el inicio de sesión
    console.log('Intentando iniciar sesión con:', this.credenciales);
  }

}




<ion-header>
  <ion-toolbar>
    <ion-buttons slot="start">
      <a routerLink="/main">
        <img src="assets/img/logo.jpeg" alt="Logo" class="logo">
      </a>
    </ion-buttons>
    <ion-title *ngIf="!showPresentation">Inicia sesión</ion-title>
    <ion-title *ngIf="showPresentation">Presentación</ion-title>
    <ion-buttons slot="end" class="cerrar">
      <ion-button *ngIf="!showPresentation" (click)="showPresentation = true">
        Atrás
      </ion-button>
      <ion-button *ngIf="showPresentation" (click)="showPresentation = false">
        Cerrar
      </ion-button>
    </ion-buttons>
  </ion-toolbar>
</ion-header>

<ion-content>
  <!-- Sección principal (Formulario de inicio de sesión) -->
  <div *ngIf="!showPresentation">
    <form>
      <div class="modal-container">
        <ion-buttons slot="start">
          <!-- Logo al lado izquierdo -->
          <img src="assets/img/logo.png" alt="Logo">
        </ion-buttons>
        <h1>GuimarBot</h1>
        <h5>Ingresar o crear una cuenta con:</h5>
        <div class="input-group">
          <ion-item>
            <ion-list>
              <br />
              <ion-input [(ngModel)]="credenciales.correo" [ngModelOptions]="{standalone: true}"
                label="Correo Electronico" label-placement="floating" fill="outline"
                placeholder="Ingrese aquí"></ion-input>
              <br />
              <ion-input [(ngModel)]="credenciales.contra" [ngModelOptions]="{standalone: true}"
                label="Contraseña" label-placement="floating" fill="outline" type="password"
                placeholder="Ingrese aquí">
                <ion-input-password-toggle slot="end"></ion-input-password-toggle>
              </ion-input>
              <br />
            </ion-list>
          </ion-item>
          <h6><ion-a [routerLink]="['/rest-contra']">¿Has olvidado la contraseña?</ion-a></h6>
        </div>
        <div>
          <ion-button expand="block" type="submit" (click)="login()">Acceder</ion-button>
        </div>
        <h6><ion-label [routerLink]="['/registrarse']">o registrate aquí</ion-label></h6>
      </div>
    </form>
  </div>

  <!-- Sección de presentación -->
  <div *ngIf="showPresentation" class="presentation">
    <h1>Bienvenido a GuimarBot</h1>
    <p>GuimarBot es una plataforma diseñada para...</p>
    <ion-button expand="block" (click)="showPresentation = false">Cerrar Presentación</ion-button>
  </div>
</ion-content>
